#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @x: - first integer
 * @y: - second integer
 * Return: Always 0 (success)
 */

int mul(int x, int y)
{
int result = x * y;
return (result);
}

