#include <stdio.h>

/**
 * main - Entry point
 * Description - 'printing numbers'
 * Return: always 0 (complete)
 */

int main(void)
{
	int h;

	for (h = 0; h <= 9; h++)
	{
		printf("%d", h);
	}
	printf("\n");
	return (0);
}
